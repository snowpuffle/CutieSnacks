package entities;

// Player Class Represents the Player Object in the Game
public class Player extends Entity {

    // Player Constructor
    public Player(String emoji, int row, int col) {
        super(emoji, row, col);
    }
}